<h1>All tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <% if task.completed %>
   [X]
  <%else %>
   [ ]
  <%end %>
  <p><%= link_to task.title, task_path(task) %></p>
  <% end %>
</ul>


<%= link_to "Add a new task", new_task_path %>
